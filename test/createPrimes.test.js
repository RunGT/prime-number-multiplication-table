const { test, expect } = require("@jest/globals");
const userInputNumber = require("../main");

test("Create an array from N", () => {
  expect(userInputNumber(1)).toStrictEqual([0, 1]);
});

test("Check numbers in the array are prime", () => {
 expect()

})
